<p id="notice"><%= notice %></p>

<h1>Survey Responses</h1>

<table>
  <thead>
    <tr>
      <td>From</td>
      <td>What do you like about Duxbury?</td>
      <td>What can be improved?</td>
    </tr>
  </thead>

  <tbody>
  <tr>
    <% @survey_responses.each do |survey_response| %>
      <tr>
        <td><%= survey_response.from %></td>
        <td><%= survey_response.question1 %></td>
        <td><%= survey_response.question2 %></td>
        <td><%= link_to 'Destroy', survey_response, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Survey Response', new_survey_response_path %>
